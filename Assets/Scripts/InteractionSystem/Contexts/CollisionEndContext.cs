using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionEndContext : CollisionContext
{
    public CollisionEndContext(Interactable interactable, Collider collider) : base(interactable, collider) { }
}
